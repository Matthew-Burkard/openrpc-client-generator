{
  "name": "{{ project_name }}",
  "version": "{{ info.version }}",
  "description": "{{ project_title }} TypeScript {{ transport }} client.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc"
  },
  "keywords": [],
  "author": "{{ info.contact.name if info.contact and info.contact.name else "" }}",
  "license": "{{ info.license_.name if info.license_ else "" }}",
{% if info.contact and info.contact.url %}
  "homepage": "{{ info.contact.url }}",
{% endif %}
  "devDependencies": {
    "@babel/preset-typescript": "^7.17.12",
    "@types/jest": "^27.5.1",
    "@types/node": "^20.5.9",
    "@types/ws": "^8.5.5",
    "prettier": "2.6.0",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "jsonrpc2-tsclient": "^2.1.1"
  }
}
